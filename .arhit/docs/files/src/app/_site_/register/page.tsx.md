# src/app/(site)/register/page.tsx

Страница /register — server component. validateSession → redirect /profile если авторизован. Рендерит RegisterForm в брендированной карточке. dynamic='force-dynamic'.
