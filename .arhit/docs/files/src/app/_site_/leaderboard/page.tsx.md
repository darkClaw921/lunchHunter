# src/app/(site)/leaderboard/page.tsx

Server component — страница лидерборда /leaderboard. Проверяет авторизацию (validateSession). Гости видят CTA на вход с Trophy иконкой. Авторизованные — LeaderboardTable. Header с кнопкой назад (→ /profile). Зависимости: validateSession, LeaderboardTable.
