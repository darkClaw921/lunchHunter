# src/app/(site)/login/_components/LoginForm.tsx

Client-форма /login. React Hook Form + zod ({email, password:min6}). POST /api/auth/login; при успехе router.replace(redirectTo из ?redirect) + router.refresh(). Серверные ошибки в alert. Ссылка на /register.
