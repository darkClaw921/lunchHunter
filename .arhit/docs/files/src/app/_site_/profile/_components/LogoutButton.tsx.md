# src/app/(site)/profile/_components/LogoutButton.tsx

Client-кнопка 'Выйти' в /profile. POST /api/auth/logout → router.replace('/') + router.refresh(). disabled во время запроса, иконка LogOut из lucide, стиль с бордером и text-error.
