# src/components/compare/CompareFAB.tsx

Плавающая кнопка-бар (Floating Action Button) сравнения. Появляется когда 1+ ланчей добавлено в сравнение. Показывает счётчик и кнопку очистки. По клику переходит на /business-lunch/compare?ids=... Позиционирована над BottomTabBar (bottom-24 mobile, bottom-6 desktop).
