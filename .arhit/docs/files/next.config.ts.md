# next.config.ts

Конфиг Next.js обёрнут withSerwistInit({swSrc:'src/app/sw.ts', swDest:'public/sw.js', cacheOnNavigation:true, reloadOnOnline:false, disable: process.env.DISABLE_PWA==='1', additionalPrecacheEntries:[{url:'/offline', revision}]}). ВАЖНО: Serwist включён и в dev (не гейтится NODE_ENV), чтобы Push API физически мог подписаться — иначе navigator.serviceWorker.ready не резолвится. Отключается флагом DISABLE_PWA=1. revision для precache: BUILD_REVISION → git HEAD → randomUUID. output:'standalone' для Docker, outputFileTracingIncludes для better-sqlite3/sharp/@node-rs/argon2. rewrite /dev-sw.js → /api/dev-sw (заглушка для старых клиентов).
